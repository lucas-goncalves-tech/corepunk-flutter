# Phase 2 Plan: Top Header & App Bar Component

## Overview
Build the standalone `CorepunkHeaderWidget` component displaying title, subtitle, shield logo icon, API status badge, and bottom border line.

## User Approval Gate
- **Gate 2.1**: User inspects and approves the top Header bar running on the Android Emulator.

## Tasks
- **Task 1**: Create `CorepunkHeaderWidget` implementing `PreferredSizeWidget`.
- **Task 2**: Connect `CorepunkHeaderWidget` to `ItemsPage` in `lib/main.dart`.
